{
  "name": "@aircury/ai-framework",
  "description": "Aircury AI Framework installer — sets up selectable architecture standards, testing, and AI agent configs in any project",
  "bin": {
    "ai-framework": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": "https://github.com/aircury/ai-framework",
  "scripts": {
    "start": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --outfile dist/cli.js --target node --sourcemap=none",
    "test": "bun test",
    "test:e2e": "bun run build && node --test e2e/cli.e2e.mjs",
    "lint": "biome check --fix",
    "format": "biome format --write"
  },
  "dependencies": {
    "@clack/prompts": "^1.2.0",
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.12",
    "@lydell/node-pty": "1.2.0-beta.14",
    "@types/bun": "^1.3.11",
    "typescript": "^6.0.2"
  }
}
