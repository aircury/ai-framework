{
  "name": "@aircury/ai-framework",
  "version": "0.1.0",
  "description": "Aircury AI Framework installer — sets up hexagonal architecture, DDD, TDD, and AI agent configs in any project",
  "bin": {
    "ai-framework": "./src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --compile --outfile dist/ai-framework --target bun",
    "test": "bun test"
  },
  "dependencies": {
    "@clack/prompts": "^1.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^6.0.2"
  }
}
